'=============================GIT============================'

git - это программное обеспечение, для контроля версий

git init -  идет инициализация нащего объекта и создается папка .git (в ней находится все необхолимые файлы нашей репозитория)

git add {file or .} - добавление файлов в рабочей папке в индекс(Индекс в Git - это специальная промежеточная область, в которой хранятся изменения фалов на пути от рабочей директории до репозитория)

git commit -m 'комментарий'- добовляет все файлы которые находятся в индексе во внутреннюю дазу данных и сохраняет их состояние на данных момент 

